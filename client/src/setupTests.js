// Το jest-dom προσθέτει custom matchers στο jest για assertions σε DOM nodes.
// Σου επιτρέπει να κάνεις πράγματα όπως:
// expect(element).toHaveTextContent(/react/i)
// Μάθε περισσότερα: https://github.com/testing-library/jest-dom
// Εισάγουμε το πακέτο για να ενεργοποιήσουμε τα matchers του jest-dom
import '@testing-library/jest-dom';
